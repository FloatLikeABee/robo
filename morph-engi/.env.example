# Local config is the repo-root `.env` (copy from `../.env.example`).
# Nested files in this folder are ignored.
#
# Unique keys (also in the root template): MORPH_ENGI_PORT, MORPH_ENGI_DATABASE_URL, MORPH_ENGI_CORS_ORIGIN

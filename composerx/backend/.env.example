# Local config is the repo-root `.env` (copy from `../../.env.example`).
# Nested files in this folder are ignored.
#
# Unique keys (also in the root template): COMPOSERX_PORT, COMPOSERX_SQLITE_PATH, COMPOSERX_BADGER_PATH

# Leave unset in dev — Vite proxies /auth, /emails, etc. to http://127.0.0.1:8043 (no CORS).
# VITE_API_BASE=

# Production / direct API access (no trailing slash):
# VITE_API_BASE=http://localhost:8043

# UsersPanel messaging drawer (optional)
# VITE_USERS_PANEL_API_URL=http://127.0.0.1:5001

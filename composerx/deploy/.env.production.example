# Copy to deploy/.env.production (gitignored). Do not put real keys in git.
# USERS_PANEL_BASE_URL is the public Morph origin, not a secret.
# Replace the empty value with https://<morph public host> (no path).
USERS_PANEL_BASE_URL=
MORPH_AI_API_KEY=
TRAN_QWEN_API_KEY=
TRAN_OPENAI_API_KEY=

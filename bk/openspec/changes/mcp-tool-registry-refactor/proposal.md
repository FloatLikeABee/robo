## Why

Refactor Ground Control's tool system so that built-in tools, custom tools, and external MCP host tools share a single registry. Unify tool discovery and invocation for both the internal agent runtime and external MCP clients.

## What Changes

- Create the `mcp-tool-registry-refactor` OpenSpec change scaffold for review and refinement.

## Capabilities

### New Capabilities
- `mcp-tool-registry-refactor`: Initial capability placeholder. Refine before implementation.

### Modified Capabilities
- None yet.

## Impact

- TBD: Fill in affected code, APIs, dependencies, or systems before implementation.

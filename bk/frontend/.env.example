# Leave unset in local dev — CRA proxies API calls to http://localhost:8000 (see package.json "proxy").
# Set when the UI is served from a different host than the API (production / custom port).
# REACT_APP_API_URL=http://localhost:8000

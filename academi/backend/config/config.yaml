server:
  port: "8080"
  mode: "debug"

database:
  path: "./data/badger"
  value_log_max_entries: 1000000

jwt:
  secret: "academi-secret-key-change-in-production"
  expiry_hours: 24

ai:
  api_key: ""
  base_url: "https://api.openai.com/v1"
  model: "gpt-4"
  max_tokens: 2048
  temperature: 0.7

cache:
  default_ttl: 3600

cors:
  origins:
    - "*"

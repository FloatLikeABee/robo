# Local config is the repo-root `.env` (copy from `../../.env.example`).
# Nested files in this folder are ignored by start-all and the FormsX process.
#
# Unique keys (also in the root template): SERVER_PORT, FORMSX_SQLITE_PATH, FORMSX_BADGER_PATH

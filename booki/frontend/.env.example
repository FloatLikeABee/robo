# Optional: API base when the UI is NOT served by Vite (preview, another host).
# During `npm run dev`, omit this variable so `/api/*` proxies to the Go server (vite.config.ts).
# VITE_API_BASE_URL=http://127.0.0.1:9095

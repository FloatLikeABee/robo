{
  "name": "robo",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "vercel-build": "npm ci --prefix morph-engi/frontend && VITE_STORAGE=local npm run build --prefix morph-engi/frontend"
  }
}
